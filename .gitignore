# Python
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/

# Environment
.env

# Local data — all SQLite databases are runtime, never committed
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/.flashrank_cache/
data/seed/embeddings_cache/

# Streamlit local cache
.streamlit/secrets.toml
.streamlit/config_local.toml

# Interview / internal context — kept locally for reference, never public
plan.md
problem_statement.md
README_BOILERPLATE.md
interview_*.md
*_interview.md

# Eval artifacts (we keep last_run.json so reviewers can see scores; ignore the temp DB)
tests/eval/__pycache__/

# IDE
.vscode/settings.json
.idea/

# Build
dist/
build/

# OS
.DS_Store
Thumbs.db
