"""Tab implementations for the Streamlit app.

Note: this directory is intentionally NOT named `pages/` — Streamlit
auto-mounts any `pages/` subdirectory next to the main script as a
multipage navigation panel in the sidebar, which conflicts with our
`st.tabs([...])`-driven layout.
"""
